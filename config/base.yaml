# Base configuration — firm-agnostic settings.
# Knobs (include_fallen_angels, group_key, utilization_format) are NOT set here.
# Each firm_*.yaml must supply all required knobs.

# Limit values are parsed from the guidelines PDF into Threshold nodes; see docs/DECISIONS.md
