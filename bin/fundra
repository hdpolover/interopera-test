#!/usr/bin/env bash
# Thin wrapper — resolves repo root from the script location so the command
# works from any directory, not just the repo root.
REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO" && DOCKER_CLI_HINTS=false docker compose --progress quiet run --rm app python -m src.cli.main "$@"
